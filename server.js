var express = require ('express');
var app = express();
var port = 5000;

var route1 = require('./.../...');
var route1 = require('./.../...');
var app = express();
app.use('/route1', route1);
app.use('/route2', route2);